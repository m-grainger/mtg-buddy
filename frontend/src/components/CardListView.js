/* import Card from "./Card";

const CardView = (props) => {
  return (
    <div>
      <ul>
        {props.cardList.map((card) => (
          <Card card={card} />
        ))}
      </ul>
    </div>
  );
};

export default CardView; */
